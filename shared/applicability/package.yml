name: 'cpe:/a:{arg}'
title: 'Package {arg} is installed'
check_id: 'package_{arg}'
template:
    name: cond_package
args:
    chrony:
        title: "Chrony package installed"
        pkgname: chrony
