<def-group>
  <definition class="inventory" id="system_boot_mode_is_bios" version="1">
    <metadata>
      <title>BIOS system boot mode check</title>
      <affected family="unix">
        <platform>multi_platform_all</platform>
      </affected>
      <description>Check if system boot mode is BIOS.</description>
      <reference ref_id="cpe:/a:non-uefi" source="CPE" />
    </metadata>
    <criteria>
      <extend_definition definition_ref="system_boot_mode_is_uefi" negate="true" comment="Pass if system boot mode is BIOS" />
    </criteria>
  </definition>
</def-group>
