{{{
oval_check_dconf_ini_file(
    path=PATH,
    section=SECTION,
    parameter=PARAMETER,
    value=VALUE,
    lock_path=LOCK_PATH,
    application="dconf", rule_id=rule_id, rule_title=rule_title)
}}}
