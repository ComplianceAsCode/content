# platform = multi_platform_all
# reboot = false
# strategy = enable
# complexity = low
# disruption = low
include install_PKGNAME

class install_PKGNAME {
  package { 'PKGNAME':
    ensure => 'installed',
  }
}
