# platform = multi_platform_all
# reboot = false
# strategy = enable
# complexity = low
# disruption = low
include install_{{{ PKGNAME }}}

class install_{{{ PKGNAME }}} {
  package { '{{{ PKGNAME }}}':
    ensure => 'installed',
  }
}
