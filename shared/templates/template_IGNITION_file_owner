# platform = multi_platform_all
# reboot = false
# strategy = configure
# complexity = low
# disruption = low
{{% if IS_DIRECTORY %}}
apiVersion: machineconfiguration.openshift.io/v1
kind: MachineConfig
spec:
  config:
    ignition:
      version: 2.2.0
    storage:
      directories:
      - filesystem: root
        user:
          id: {{{ FILEUID }}}
        path: {{{ FILEPATH }}}
{{% else %}}
apiVersion: machineconfiguration.openshift.io/v1
kind: MachineConfig
spec:
  config:
    ignition:
      version: 2.2.0
    storage:
      files:
      - filesystem: root
        user:
          id: {{{ FILEUID }}}
        path: {{{ FILEPATH }}}
{{% endif %}}
