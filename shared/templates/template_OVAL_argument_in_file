{{{
    oval_argument_in_file(
        filepath=FILEPATH,
        name=ARG_NAME,
        value=ARG_VALUE,
        application=APPLICATION, #'BLS-compatible boot loader for Linux operating system'
        line_prefix=LINE_PREFIX,
        line_suffix=LINE_SUFFIX
    )
}}}
