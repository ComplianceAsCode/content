# platform = multi_platform_all
# reboot = false
# strategy = restrict
# complexity = low
# disruption = low

- name: "Make sure contents of {{{ FILEPATH }}} are as expected"
  copy:
    dest: "{{{ FILEPATH }}}"
    content: |+
        {{{ CONTENTS|indent(8) }}}
    force: yes
