# platform = multi_platform_rhel
# reboot = false
# strategy = restrict
# complexity = medium
# disruption = medium

- name: Set /etc/gshadow group ownership to root
  file:
    path: /etc/gshadow
    group: root
  tags:
    @ANSIBLE_TAGS@

