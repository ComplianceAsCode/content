# platform=multi_platform_rhel,multi_platform_fedora
# reboot = false
# strategy = restrict
# complexity = medium
# disruption = medium
- name: Verify Group Who Owns /etc/cron.allow file
  file:
    path: /etc/cron.allow
    group: root
  tags:
    @ANSIBLE_TAGS@
