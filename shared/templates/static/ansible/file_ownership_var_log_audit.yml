# platform=multi_platform_rhel,multi_platform_fedora
# reboot = false
# strategy = restrict
# complexity = medium
# disruption = medium
- name: "Set ownership to root of /var/log/audit/ and associated files"
  file: dest=/var/log/audit/ owner=root recurse=yes
  tags:
    @ANSIBLE_TAGS@
