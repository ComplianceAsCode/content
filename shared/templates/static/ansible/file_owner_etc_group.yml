# platform = multi_platform_rhel
# reboot = false
# strategy = restrict
# complexity = medium
# disruption = medium

- name: Set /etc/group user ownership to root
  file:
    path: /etc/group
    owner: root
  tags:
    @ANSIBLE_TAGS@

