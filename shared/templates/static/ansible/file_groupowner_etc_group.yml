# platform = multi_platform_rhel
# reboot = false
# strategy = restrict
# complexity = medium
# disruption = medium

- name: Set /etc/group group ownership to root
  file:
    path: /etc/group
    group: root
  tags:
    @ANSIBLE_TAGS@

