# platform = multi_platform_all
# reboot = false
# strategy = disable
# complexity = low
# disruption = low
include remove_PKGNAME

class remove_PKGNAME {
  package { 'PKGNAME':
    ensure => 'purged',
  }
}
