# platform = %PLATFORMS%
# reboot = false
# strategy = restrict
# complexity = medium
# disruption = medium

- name: Set %PATH% permissions
  file:
    path: %PATH%
    mode: %MODE%
  tags:
    @ANSIBLE_TAGS@

