# platform = multi_platform_all
# reboot = false
# strategy = configure
# complexity = low
# disruption = low
find {{{ FILEPATH }}} -regex '^{{{ FILEPATH }}}/{{{ FILENAME }}}' -exec chmod {{{ FILEMODE }}} {} \;
