# platform = multi_platform_all
# reboot = false
# strategy = restrict
# complexity = low
# disruption = low
{{{ ansible_lineinfile(msg='', path=PATH, new_line=TEXT, regex=REGEX, create='yes', state='present', insert_after='', insert_before='', rule_title=rule_title) -}}}
