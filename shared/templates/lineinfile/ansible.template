# platform = multi_platform_all
# reboot = false
# strategy = restrict
# complexity = low
# disruption = low

{{{ ansible_lineinfile(msg='', path=PATH, new_line=TEXT, create='yes', state='present', insert_after='', insert_before='') -}}}

