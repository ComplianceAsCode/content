# platform = %PLATFORMS%
# reboot = false
# strategy = restrict
# complexity = medium
# disruption = medium

- name: Set %PATH% group ownership to root
  file:
    path: %PATH%
    group: root
  tags:
    @ANSIBLE_TAGS@

