# platform = multi_platform_all
# reboot = false
# strategy = enable
# complexity = low
# disruption = low
- name: Ensure {{{ PKGNAME }}} is installed
  ansible.builtin.package:
    name: "{{{ PKGNAME }}}"
    state: present

