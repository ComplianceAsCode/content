# platform = multi_platform_rhel,multi_platform_fedora,multi_platform_ol,multi_platform_rhv,multi_platform_sle
# reboot = false
# strategy = enable
# complexity = low
# disruption = low
. /usr/share/scap-security-guide/remediation_functions

package_install '{{{ PACKAGENAME }}}' || exit 1

{{%- if init_system == "systemd" %}}

SYSTEMCTL_EXEC='/usr/bin/systemctl'
"$SYSTEMCTL_EXEC" start '{{{ SERVICENAME }}}.service'
"$SYSTEMCTL_EXEC" enable '{{{ SERVICENAME }}}.service'
{{% elif init_system == "upstart" %}}

/sbin/service '{{{ SERVICENAME }}}' disable
/sbin/chkconfig --level 0123456 '{{{ SERVICENAME }}}' off
{{% else %}}
JINJA TEMPLATE ERROR: Unknown init system '{{{ init_system }}}'
{{%- endif %}}
