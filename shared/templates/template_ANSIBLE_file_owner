# platform = %PLATFORMS%
# reboot = false
# strategy = restrict
# complexity = medium
# disruption = medium

- name: Set %PATH% user ownership to root
  file:
    path: %PATH%
    owner: root
  tags:
    @ANSIBLE_TAGS@

