summary:
  Destructive STIG profile test (Bash)
discover+:
  test:
  - /hardening/oscap/(ospp|stig)$
adjust+:
- enabled: false
  when: distro == fedora
provision:
  hardware:
    virtualization:
      is-supported: true
