documentation_complete: true

prodtype: fedora,ol7,ol8,rhcos4,rhel7,rhel8,rhv4,sle12,sle15

title: 'remove iptables Package'

description: |-
    {{{ describe_package_remove(package="iptables") }}}

rationale: |-
    <tt>iptables</tt> controls the Linux kernel network packet filtering
    code. <tt>iptables</tt> allows system operators to set up firewalls and IP
    masquerading, etc.

severity: medium

identifiers:
    #NO REFERENCES AVAILABLE

references:
    nist: CM-6(a)
    srg: SRG-OS-000480-GPOS-00227
    cis@rhel8: 3.4.1.1

ocil_clause: 'the package is installed'

ocil: '{{{ ocil_package(package="iptables") }}}'

template:
    name: package_removed
    vars:
        pkgname: iptables
