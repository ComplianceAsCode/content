documentation_complete: true

prodtype: rhel6,rhel7,rhel8,fedora,ol7,ol8,rhv4

title: 'Install binutils Package'

description: |-
    {{{ describe_package_install(package="binutils") }}}

rationale: |-
    <tt>binutils</tt> is a collection of binary utilities required for
    foundational system operator activities, such as <tt>ld</tt>,
    <tt>nm</tt>, <tt>objcopy</tt> and <tt>readelf</tt>.

severity: medium

identifiers:
    cce@rhel6: 82991-1
    cce@rhel7: 82990-3
    cce@rhel8: 82989-5

ocil_clause: 'the package is not installed'

ocil: '{{{ ocil_package(package="binutils") }}}'

template:
    name: package_installed
    vars:
        pkgname: binutils
