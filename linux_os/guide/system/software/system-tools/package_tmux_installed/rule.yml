documentation_complete: true

prodtype: rhel6,rhel7,rhel8,fedora,ol7,ol8,rhv4

title: 'Install tmux Package'

description: |-
    {{{ describe_package_install(package="tmux") }}}

rationale: |-
    <tt>tmux</tt> is a "terminal multiplexer." It allows a number of terminals
    (or windows) to be accessed and controlled from a single terminal. <tt>tmux</tt> is
    intended to be a simple, modern, BSD-licensed alternative to programs such as GNU
    screen.

severity: medium

identifiers:
    cce@rhel6: 82964-8
    cce@rhel7: 82963-0
    cce@rhel8: 82962-2

ocil_clause: 'the package is not installed'

ocil: '{{{ ocil_package(package="tmux") }}}'
