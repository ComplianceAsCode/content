documentation_complete: true

title: Ensure GCC compiler is installed

description: The gcc package should be installed.

rationale: GCC can compile programs to binary executables

template:
    name: package_installed
    vars:
        pkgname: gcc
        evr: 0:9.2.1-1
    backends:
        bash: off

severity: low
