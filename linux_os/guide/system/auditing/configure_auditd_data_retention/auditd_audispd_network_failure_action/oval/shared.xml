<def-group>
  <definition class="compliance" id="auditd_audispd_network_failure_action" version="1">
    <metadata>
      <title>Configure audispd's Plugin network_failure_action On Network Failure</title>
      <affected family="unix">
        <platform>multi_platform_sle</platform>
      </affected>
      <description>network_failure_action setting in /etc/audisp/audisp-remote.conf is set to an acceptable value</description>
    </metadata>
    <criteria>
        <criterion comment="network_failure_action setting in audisp-remote.conf" test_ref="test_auditd_audispd_configure_network_failure_action" />
    </criteria>
  </definition>

  <ind:textfilecontent54_test check="all" comment="remote server to send audit records" id="test_auditd_audispd_configure_network_failure_action" version="1">
    <ind:object object_ref="object_auditd_audispd_configure_network_failure_action" />
    <ind:state state_ref="state_auditd_audispd_configure_network_failure_action" />
  </ind:textfilecontent54_test>

  <ind:textfilecontent54_object id="object_auditd_audispd_configure_network_failure_action" version="1">
    <ind:filepath>/etc/audisp/audisp-remote.conf</ind:filepath>
    <!-- Allow only space (exactly) as delimiter -->
    <!-- Require at least one space before and after the equal sign -->
    <ind:pattern operation="pattern match">^[ ]*network_failure_action[ ]+=[ ]+(\S+)[ ]*$</ind:pattern>
    <ind:instance datatype="int">1</ind:instance>
  </ind:textfilecontent54_object>

  <ind:textfilecontent54_state id="state_auditd_audispd_configure_network_failure_action" version="1">
      <ind:subexpression operation="pattern match">^(?:single|syslog|halt)$</ind:subexpression>
  </ind:textfilecontent54_state>

</def-group>
