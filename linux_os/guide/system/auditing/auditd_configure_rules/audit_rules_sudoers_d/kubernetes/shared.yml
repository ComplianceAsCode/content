---
# platform = multi_platform_fedora,multi_platform_rhcos
# reboot = true
# strategy = restrict
# complexity = low
# disruption = low

{{{ kubernetes_machineconfig_audit_add_watch_rule(path="/etc/sudoers.d/", permissions="wa", key="identity") }}}% 
