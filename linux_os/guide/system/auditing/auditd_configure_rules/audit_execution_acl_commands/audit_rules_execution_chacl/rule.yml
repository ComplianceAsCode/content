documentation_complete: true

prodtype: ol8,rhel8,rhel9,sle12,sle15,ubuntu2004

title: 'Record Any Attempts to Run chacl'

description: |-
    At a minimum, the audit system should collect any execution attempt
    of the <tt>chacl</tt> command for all users and root. If the <tt>auditd</tt>
    daemon is configured to use the <tt>augenrules</tt> program to read audit rules
    during daemon startup (the default), add the following lines to a file with suffix
    <tt>.rules</tt> in the directory <tt>/etc/audit/rules.d</tt>:
    <pre>-a always,exit -F path=/usr/bin/chacl -F perm=x -F auid&gt;={{{ auid }}} -F auid!=unset -F key=privileged</pre>
    If the <tt>auditd</tt> daemon is configured to use the <tt>auditctl</tt>
    utility to read audit rules during daemon startup, add the following lines to
    <tt>/etc/audit/audit.rules</tt> file:
    <pre>-a always,exit -F path=/usr/bin/chacl -F perm=x -F auid&gt;={{{ auid }}} -F auid!=unset -F key=privileged</pre>

rationale: |-
    Without generating audit records that are specific to the security and
    mission needs of the organization, it would be difficult to establish,
    correlate, and investigate the events relating to an incident or identify
    those responsible for one.
    Audit records can be generated from various components within the
    information system (e.g., module or policy filter).

severity: medium

identifiers:
    cce@rhel8: CCE-89446-9
    cce@rhel9: CCE-87685-4
    cce@sle12: CCE-83190-9
    cce@sle15: CCE-85595-7

references:
    disa: CCI-000130,CCI-000135,CCI-000169,CCI-000172,CCI-002884
    nist@sle12: AU-3,AU-3.1,AU-12.1(ii),AU-12(a),AU-12.1(iv),AU-12(c),MA-4(1)(a)
    srg: SRG-OS-000037-GPOS-00015,SRG-OS-000042-GPOS-00020,SRG-OS-000062-GPOS-00031,SRG-OS-000392-GPOS-00172,SRG-OS-000462-GPOS-00206,SRG-OS-000471-GPOS-00215,SRG-OS-000466-GPOS-00210
    stigid@ol8: OL08-00-030570
    stigid@rhel8: RHEL-08-030570
    stigid@sle12: SLES-12-020620
    stigid@sle15: SLES-15-030440
    stigid@ubuntu2004: UBTU-20-010168

ocil_clause: '{{{ ocil_clause_audit() }}}'

ocil: |-
    Verify {{{ full_name }}} generates an audit record for all uses of the "chacl" command.
    To verify that execution of the command is being audited, run the following command:
    $ sudo grep chacl /etc/audit/rules.d/audit.*
    The output should be similar to:
    -a always,exit -F path=/usr/bin/chacl -F perm=x -F auid&gt;={{{ auid }}} -F auid!=unset -F key=perm_mod

fixtext: |-
    {{{ fixtext_audit_rules_command("chacl", "/usr/bin/chacl", "perm_mod") | indent(4) }}}

srg_requirement: '{{{ srg_requirement_audit_syscall("chacl") }}}'

template:
    name: audit_rules_privileged_commands
    vars:
        path: /usr/bin/chacl
