<def-group>
	<definition class="compliance" id="{{{ rule_id }}}" version="1">
    {{{ oval_metadata("Check that authselect is enabled") }}}
    <criteria operator="AND" comment="Check that /etc/authselect/authselect.conf is not empty">
      <criterion comment="Check that /etc/authselect/authselect.conf is not empty" test_ref="test_authselect_conf_not_empty" />
    </criteria>
  </definition>

  <ind:textfilecontent54_test check="all"
    comment="Check that /etc/authselect/authselect.conf is not empty"
    id="test_authselect_conf_not_empty" version="1">
  <ind:object object_ref="obj_authselect_conf_not_empty" />
  </ind:textfilecontent54_test>

  <ind:textfilecontent54_object id="obj_authselect_conf_not_empty" version="1">
    <ind:filepath>/etc/authselect/authselect.conf</ind:filepath>
    <ind:pattern operation="pattern match">^.+$</ind:pattern>
    <ind:instance operation="greater than or equal" datatype="int">1</ind:instance>
  </ind:textfilecontent54_object>
</def-group>

