# platform = multi_platform_all
# reboot = false
# strategy = restrict
# complexity = low
# disruption = low

{{{ ansible_set_config_file(msg='Ensure new users receive home directories', file='/etc/login.defs', parameter="CREATE_HOME", value='yes', create='yes') }}}
