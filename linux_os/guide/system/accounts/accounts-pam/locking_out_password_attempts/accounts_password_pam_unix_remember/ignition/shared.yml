# platform = multi_platform_rhel,multi_platform_fedora,multi_platform_ocp
apiVersion: machineconfiguration.openshift.io/v1
kind: MachineConfig
spec:
  config:
    ignition:
      version: 2.2.0
    storage:
      files: 
      - contents: data:,remember%3D%20%0A{{{<sub idref="var_password_pam_unix_remember" /> }}}
        mode: 644
        path: /etc/pam.d/system-auth        
        filesystem: root
