# platform = multi_platform_rhel,multi_platform_fedora,multi_platform_ol,multi_platform_rhv,multi_platform_sle
# reboot = false
# strategy = configure
# complexity = low
# disruption = medium

{{{ ansible_instantiate_variables("var_password_pam_remember",
                                  "var_password_pam_remember_control_flag") }}}

{{{ ansible_pam_pwhistory_enable('/etc/pam.d/system-auth',
                                 '{{ var_password_pam_remember_control_flag.split(",")[0] }}',
                                 '^password.*requisite.*pam_pwquality\.so') }}}

{{{ ansible_pam_pwhistory_parameter_value('/etc/pam.d/system-auth',
                                          'remember',
                                          '{{ var_password_pam_remember }}') }}}
