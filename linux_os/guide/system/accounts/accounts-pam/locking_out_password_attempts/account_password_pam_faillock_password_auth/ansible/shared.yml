# platform = multi_platform_all
# reboot = false
# strategy = enable
# complexity = low
# disruption = low

{{{ ansible_authselect_force_reselect(rule_title=rule_title) }}}
{{{ ansible_pam_faillock_enable(rule_title=rule_title) }}}
