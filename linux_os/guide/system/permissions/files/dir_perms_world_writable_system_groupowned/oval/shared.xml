<def-group>
  <definition class="compliance" id="dir_perms_world_writable_system_groupowned" version="1">
    <metadata>
      <title>Find world writable directories not group-owned by an application group</title>
      <affected family="unix">
        <platform>multi_platform_sle</platform>
      </affected>
      <description>All world writable directories should be groupowned by an application group.</description>
    </metadata>
    <criteria comment="check for local directories that are world writable and have gid greater than or equal to 1000" negate="true">
      <criterion comment="check for local directories that are world writable and have gid greater than or equal to 1000" test_ref="test_dir_world_writable_gid_gt_value" />
    </criteria>
  </definition>
  <unix:file_test check="all" comment="check for local directories that are world writable and have gid greater than or equal to 1000" id="test_dir_world_writable_gid_gt_value" version="1">
    <unix:object object_ref="all_local_directories" />
    <unix:state state_ref="state_gid_is_interactive" />
  </unix:file_test>
  <unix:file_object comment="all local directories" id="all_local_directories" version="1">
    <unix:behaviors recurse="directories" recurse_direction="down" max_depth="-1" recurse_file_system="local" />
    <unix:path operation="equals">/</unix:path>
    <unix:filename xsi:nil="true" />
    <filter action="include">state_is_world_writable</filter>
  </unix:file_object>
  <unix:file_state comment="is world writable" id="state_is_world_writable" version="1">
    <unix:owrite datatype="boolean">true</unix:owrite>
  </unix:file_state>
  <unix:file_state comment="gid greater than or equal to 1000" id="state_gid_is_interactive" version="1">
    <unix:group_id datatype="int" operation="greater than or equal">1000</unix:group_id>
  </unix:file_state>
</def-group>
