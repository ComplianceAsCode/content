documentation_complete: true

title: Disable the deny_bluetooth SELinux Boolean

description: |-
    By default, the SELinux boolean <tt>deny_bluetooth</tt> is disabled.
    If this setting is enabled, it should be disabled.
    {{{ describe_sebool_disable(sebool="deny_bluetooth") }}}

rationale: ""

severity: medium

{{{ complete_ocil_entry_sebool_disabled(sebool="deny_bluetooth") }}}

template:
    name: sebool
    vars:
        seboolid: deny_bluetooth
