documentation_complete: true


title: 'Disable the use_samba_home_dirs SELinux Boolean'

description: |-
    By default, the SELinux boolean <tt>use_samba_home_dirs</tt> is disabled.
    If this setting is enabled, it should be disabled.
    {{{ describe_sebool_disable(sebool="use_samba_home_dirs") }}}

rationale: ""

severity: medium

{{{ complete_ocil_entry_sebool_disabled(sebool="use_samba_home_dirs") }}}

template:
    name: sebool
    vars:
        seboolid: use_samba_home_dirs
