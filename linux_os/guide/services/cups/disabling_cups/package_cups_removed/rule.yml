documentation_complete: true

prodtype: rhel7,rhel8,sle12,sle15

title: 'Uninstall CUPS Server Package'

description: |-
    If the system does not need to act as a Print server,
    the cups package can be uninstalled.
    {{{ describe_package_remove(package="dhcp") }}}

rationale: |-
    Removing the CUPS server ensures that it cannot be easily or
    accidentally reactivated and disrupt network operation.

severity: medium

references:
    cis@rhel7: 2.2.4
    cis@rhel8: 2.2.16
    cis@sle12: 2.2.4
    cis@sle15: 2.2.4

{{{ complete_ocil_entry_package(package="cups") }}}

template:
    name: package_removed
    vars:
        pkgname: cups
