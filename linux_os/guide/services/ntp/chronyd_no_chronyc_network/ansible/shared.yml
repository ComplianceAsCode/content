# platform = multi_platform_all
# reboot = false
# strategy = restrict
# complexity = low
# disruption = low

{{{ ansible_set_config_file(file=chrony_conf_path, parameter='cmdport', separator=' ', value='0', create='yes') }}}
