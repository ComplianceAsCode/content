<def-group>
    <definition class="compliance" id="{{{rule_id}}}" version="1">
        {{{ oval_metadata("This test makes sure that /etc/cron.deny doesn't exist.") }}}
        <criteria>
            <criterion comment="Ensure that /etc/cron.deny does not exist" test_ref="test_{{{rule_id}}}" />
        </criteria>
    </definition>

    <unix:file_test id="test_{{{rule_id}}}" check="all" check_existence="none_exist" version="1" comment="Test that that /etc/cron.deny does not exist">
        <unix:object object_ref="object_{{{rule_id}}}" />
    </unix:file_test>

    <unix:file_object comment="/etc/cron.deny" id="object_{{{rule_id}}}" version="1">
        <unix:filepath>/etc/cron.deny</unix:filepath>
    </unix:file_object>
</def-group>
