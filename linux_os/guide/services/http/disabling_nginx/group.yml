documentation_complete: true

title: 'Disable Nginx if Possible'

description: |-
    If Nginx was installed and activated, but the system
    does not need to act as a web server, then it should be disabled
    and removed from the system.
