documentation_complete: true

prodtype: rhcos4

title: 'Verify User Who Owns The OpenShift Kubelet Service File'

description: '{{{ describe_file_owner(file="/etc/systemd/system/kubelet.service", owner="root") }}}'

rationale: |-
    The <tt>/etc/systemd/system/kubelet.service</tt> file contains information about the configuration of the
    OpenShift kubelet service that is configured on the system. Protection of this file is
    critical for OpenShift security.

severity: medium

references:
    cis: 4.1.2

ocil_clause: '{{{ ocil_clause_file_owner(file="/etc/systemd/system/kubelet.service", owner="root") }}}'

ocil: '{{{ ocil_file_owner(file="/etc/systemd/system/kubelet.service", owner="root") }}}'

template:
    name: file_owner
    vars:
        filepath: /etc/systemd/system/kubelet.service
        fileuid: '0'
