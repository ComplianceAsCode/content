documentation_complete: true

prodtype: rhel7,rhel8,sle12,sle15

title: 'Disable rsync Service'

description: '{{{ describe_service_disable(service="rsync") }}}'

rationale: |-
    Running rsync provides a network-based avenue
    of attack, and should be disabled if not needed.

severity: unknown

references:
    cis@rhel7: 2.2.21
    cis@rhel8: 2.2.3
    cis@sle12: 2.2.18
    cis@sle15: 2.2.17

ocil: '{{{ ocil_service_disabled(service="rsync") }}}'

platform: machine

template:
    name: service_disabled
    vars:
        servicename: rsync
