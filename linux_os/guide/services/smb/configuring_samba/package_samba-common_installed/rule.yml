documentation_complete: true

prodtype: ale12,multi_platform_sle,rhel6,rhel7,rhel8,sle11,sle12

title: 'Install the Samba Common Package'

description: |-
    The <tt>samba-common</tt> package should be installed.
    {{{ describe_package_install(package="samba-common") }}}

rationale: 'If the samba-common package is not installed, samba cannot be configured.'

severity: medium

identifiers:
    cce@rhel7: 80360-1

ocil_clause: 'the package is not installed'

ocil: '{{{ ocil_package(package="samba-common") }}}'

template:
    name: package_installed
    vars:
        pkgname: samba-common
