documentation_complete: true

title: 'Remote server for audispd to send audit records'

description: |-
    For "rhel8" and "fedora" product, the setting for remote_server in /etc/audit/audisp-remote.conf
    For other products, the setting for remote_server in /etc/audisp/audisp-remote.conf

type: string

interactive: true

options:
    default: logcollector
