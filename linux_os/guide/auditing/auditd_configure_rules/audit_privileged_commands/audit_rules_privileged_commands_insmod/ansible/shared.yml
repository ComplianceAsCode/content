# platform = multi_platform_rhel,multi_platform_sle,multi_platform_slmicro
# reboot = false
# strategy = restrict
# complexity = low
# disruption = low

{{{ ansible_audit_augenrules_add_watch_rule(path='/sbin/insmod', permissions='x', key='modules', rule_title=rule_title) }}}
{{{ ansible_audit_auditctl_add_watch_rule(path='/sbin/insmod', permissions='x', key='modules', rule_title=rule_title) }}}
