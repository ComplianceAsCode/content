documentation_complete: true

title: 'Configure <tt>iptables</tt> to Allow Access to the Web Server'

description: |-
    By default, <tt>iptables</tt>
    blocks access to the ports used by the web server.
    <iptables-desc-macro allow="true" port="80" proto="tcp" />
    <iptables-desc-macro allow="true" port="443" proto="tcp" />
