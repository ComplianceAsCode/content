<def-group>
  <definition class="compliance" id="mount_home_own_partition" version="1">
    <metadata>
      <title>Ensure /home Located On Separate Partition</title>
      <affected family="unix">
        <platform>Red Hat Enterprise Linux 6</platform>
      </affected>
      <reference ref_id="CCE-14559-9" source="CCE" />
      <description>If user home directories will be stored locally,
      create a separate partition for /home. If /home will be mounted
      from another system such as an NFS server, then creating a separate
      partition is not necessary at this time, and the mountpoint can
      instead be configured later.</description>
    </metadata>
    <criteria operator="AND">
      <criterion test_ref="test_home_partition" comment="check for /home partition" />
      <criterion test_ref="test_etc_fstab_home" comment="find /home in /etc/fstab" />
    </criteria>
  </definition>

  <linux:partition_test check="all" check_existence="all_exist" id="test_home_partition" version="1" comment="check for /home partition">
      <linux:object object_ref="object_home_partition" />
  </linux:partition_test>

  <linux:partition_object id="object_home_partition" version="1">
      <linux:mount_point>/home</linux:mount_point>
  </linux:partition_object>

  <ind:textfilecontent54_test check="all" check_existence="all_exist" comment="find /home in /etc/fstab" id="test_etc_fstab_home" version="1">
    <ind:object object_ref="object_etc_fstab_home" />
  </ind:textfilecontent54_test>

  <ind:textfilecontent54_object id="object_etc_fstab_home" version="1" comment="find /home in /etc/fstab">
    <ind:path>/etc</ind:path>
    <ind:filename>fstab</ind:filename>
    <ind:pattern operation="pattern match">^.*\s/home\s.*$</ind:pattern>
    <ind:instance datatype="int" operation="equals">1</ind:instance>
  </ind:textfilecontent54_object>
</def-group>
