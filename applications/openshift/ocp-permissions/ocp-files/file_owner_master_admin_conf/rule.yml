documentation_complete: true

prodtype: ocp3

title: 'Verify User Who Owns The OpenShift Admin Kubeconfig File'

description: '{{{ describe_file_owner(file="/etc/origin/master/admin.kubeconfig", owner="root") }}}'

rationale: |-
    The <tt>/etc/origin/master/admin.kubeconfig</tt> file contains information about the administrative configuration of the
    OpenShift cluster that is configured on the system. Protection of this file is
    critical for OpenShift security.

severity: medium

identifiers:
    cce@ocp3: 80622-4

references:
    cis: 1.4.14

ocil_clause: '{{{ ocil_clause_file_owner(file="/etc/origin/master/admin.kubeconfig", owner="root") }}}'

ocil: '{{{ ocil_file_owner(file="/etc/origin/master/admin.kubeconfig", owner="root") }}}'

template:
    name: file_owner
    vars:
        filepath: /etc/origin/master/admin.kubeconfig
        fileuid: '0'
