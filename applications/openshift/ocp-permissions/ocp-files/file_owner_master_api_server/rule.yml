documentation_complete: true

prodtype: ocp3

title: 'Verify User Who Owns The OpenShift API Specification File'

description: '{{{ describe_file_owner(file="/etc/origin/node/pods/apiserver.yaml", owner="root") }}}'

rationale: |-
    The <tt>/etc/origin/node/pods/apiserver.yaml</tt> file contains information about the configuration of the
    OpenShift API Server that is configured on the system. Protection of this file is
    critical for OpenShift security.

severity: medium

identifiers:
    cce@ocp3: 82058-9

references:
    cis: 1.4.2

ocil_clause: '{{{ ocil_clause_file_owner(file="/etc/origin/node/pods/apiserver.yaml", owner="root") }}}'

ocil: '{{{ ocil_file_owner(file="/etc/origin/node/pods/apiserver.yaml", owner="root") }}}'
