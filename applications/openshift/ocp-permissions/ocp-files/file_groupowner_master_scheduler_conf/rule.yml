documentation_complete: true

prodtype: ocp3

title: 'Verify Group Who Owns The OpenShift Scheduler Configuration File'

description: '{{{ describe_file_group_owner(file="/etc/origin/master/scheduler.json", group="root") }}}'

rationale: |-
    The <tt>/etc/origin/master/scheduler.json</tt> file contains information about the configuration of the
    OpenShift scheduler that is configured on the system. Protection of this file is
    critical for OpenShift security.

severity: medium

identifiers:
    cce@ocp3: 80616-6

references:
    cis: 1.4.16

ocil_clause: '{{{ ocil_clause_file_group_owner(file="/etc/origin/master/scheduler.json", group="root") }}}'

ocil: '{{{ ocil_file_group_owner(file="/etc/origin/master/scheduler.json", group="root") }}}'

template:
    name: file_groupowner
    vars:
        filepath: /etc/origin/master/scheduler.json
        filegid: '0'
