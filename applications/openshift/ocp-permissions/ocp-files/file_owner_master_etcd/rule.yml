documentation_complete: true

prodtype: ocp3

title: 'Verify User Who Owns The OpenShift etcd Specification File'

description: '{{{ describe_file_owner(file="/etc/origin/node/pods/etcd.yaml", owner="root") }}}'

rationale: |-
    The <tt>/etc/origin/node/pods/etcd.yaml</tt> file contains information about the configuration of the
    OpenShift etcd Server that is configured on the system. Protection of this file is
    critical for OpenShift security.

severity: medium

identifiers:
    cce@ocp3: CCE-80625-7

references:
    cis: 1.4.8

ocil_clause: '{{{ ocil_clause_file_owner(file="/etc/origin/node/pods/etcd.yaml", owner="root") }}}'

ocil: '{{{ ocil_file_owner(file="/etc/origin/node/pods/etcd.yaml", owner="root") }}}'

template:
    name: file_owner
    vars:
        filepath: /etc/origin/node/pods/etcd.yaml
        fileuid: '0'
