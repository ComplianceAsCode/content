documentation_complete: true

prodtype: ocp3

title: 'Verify User Who Owns The OpenShift Configuration Directory'

description: '{{{ describe_file_owner(file="/etc/origin/", owner="root") }}}'

rationale: 'If users can modify the OpenShift configurations, the OpenShift cluster can become inoperable or compromised'

severity: medium

ocil_clause: '{{{ ocil_clause_file_owner(file="/etc/origin/", owner="root") }}}'

ocil: '{{{ ocil_file_owner(file="/etc/origin/", owner="root") }}}'

template:
    name: file_owner
    vars:
        filepath: /etc/origin/
        fileuid: '0'
