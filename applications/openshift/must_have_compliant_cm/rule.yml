prodtype: ocp4

title: Must have compliant CM

description: The deployment must have a CM that's compliant with.... life.

rationale: TBD

identifiers: {}

severity: unknown

warnings:
- general: |-
    {{{ openshift_cluster_setting("/api/v1/namespaces/openshift/configmaps/my-compliance-configmap") | indent(4) }}}

template:
  name: yamlfile_value
  vars:
    ocp_data: "true"
    entity_check: "at least one"
    filepath: /api/v1/namespaces/openshift/configmaps/my-compliance-configmap
    yamlpath: ".data.compliant"
    values:
    - value: "yep"
